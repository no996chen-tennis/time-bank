# 进度报告 · 留存闭环 P0 实现（2026-06-11）

> Claude 自主长任务产出（Adam 睡眠时执行）。分支 `claude/retention-p0`（从 `m3-app-loop` 切出），**全程未 push、未动 main**。
> 设计总纲见 [`设计-三位一体闭环-2026-06-11.md`](设计-三位一体闭环-2026-06-11.md)。

---

## 一、一句话总结

把 Adam 拍板保留的三套（叁 记忆盲盒/明信片 · 肆 星空账本 · 伍 签到台）**串成了一个闭环并落进真实代码**：阶段一产出统一设计文档，阶段二把 Step 0–6 全部实现、**每步独立构建通过并 commit**，App 在 iPhone 17 Pro 模拟器启动稳定、一键存入端到端验证成功。

---

## 二、闭环设计摘要

```
① 签到台/Widget ──► ② 存入 ──► ③ 星空(每笔点亮一颗星)
   (触发/入口)        (投入)          │
        ▲                            ▼ (数日沉淀)
        │                  ④ 盲盒回看 + 明信片冲洗
   ⑤ 回信(再投入) ◄────────────┘ (不可预测的回流)
        └──────────► 回到 ②，飞轮再转一圈
```

一切都是 `Moment`（SwiftData）→ `WidgetSnapshotWriter` 投影成 app-group JSON 快照 → Widget 渲染。三套不是三个 tab，而是同一份数据在五个工位的不同呈现。完整闭环图 / 数据契约 / 连续奖励表 / Widget 轮换物理极限 / 冷启动策略，全部在设计文档里回答。

---

## 三、完成的步骤（8 个 commit）

| commit | 步骤 | 内容 |
|--------|------|------|
| `0a7228a` | Step 0 | **方案壹存折主页视觉**（工作树已有改动，落到分支） |
| `34edf59` | 阶段一 | **五方案调研 + 三位一体闭环统一设计文档** |
| `e0336fd` | Step 1 | **Widget 全家桶**：systemMedium + systemSmall + 锁屏 rect；今日存入状态机（凌晨重置→白天平静→18点温柔催→21:30睡前邀请→存入后 ✦）；内容轮换（timeline 预排 48 entry，零刷新预算）；配色走 token 表 |
| `8ab328a` | Step 2 | **一键存入**：widget App Intent 写 app-group 队列 + 乐观翻转"今日已存入" + reload；App 回前台 drain 队列建真 Moment（首个可写账户/此刻/时长0），幂等。**不迁移 store，历史数据不丢** |
| `3e8a325` | Step 3 | **今日回忆卡（盲盒）+ 回信 + 冷启动**：日期种子稳定选片（那年今日优先）；主页 hero 下卡片；详情页「现在的我想说」回信；Moment 加 reply/repliedAt/postcardSeenAt |
| `49ed322` | Step 4 | **明信片冲洗**：PostcardView（照片+邮票+邮戳+脚注）；本地通知 2-4 天（每周≤4、今晚已存入则顺延、有候选才请求权限）；通知点开路由到明信片 |
| `b5b9636` | Step 5 | **星空账本**：账户 Tab 入口卡 + StarFieldView（Canvas）；每账户一星座、每瞬间一颗星，只画已点亮；twinkle + 连续活跃时流星 |
| `6a46890` | Step 6 | **连续奖励**：点亮日历 + 最近八周点亮/留白条（自动补灯）+ 里程碑 10/50/100 庆典页。只奖不罚 |

**构建**：每步 `xcodebuild -scheme TimeBank` 均 **BUILD SUCCEEDED**（含 widget 扩展）。
**测试**：`xcodebuild test` 末尾 **TEST SUCCEEDED**（单元测试通过；UI 测试 runner 有一次启动失败的告警，疑似被通知权限弹窗挡住 SpringBoard，不阻塞）。

---

## 四、截图索引（`designs/2026-06-11-P0实现截图/`）

| 文件 | 说明 |
|------|------|
| `step1-app-home-passbook.png` | 方案壹存折主页（hero 大数字 + 人生进度条 + 已存入条）正常渲染 |
| `step2-onetap-deposit-drained.png` | **一键存入端到端验证**：注入队列→启动→落库成「陪伴侣 1 个瞬间」、队列清空、快照 todayDeposited=true |
| `step3-home-memory-card.png` | 今日回忆卡（命中今天的「此刻」） |
| `step3-home-anniversary.png` | **今日回忆卡命中「一年前的今天 · 去年初夏的傍晚散步」**（那年今日路径） |
| `step4-launch-notif-permission.png` | 存在 2-4 天前未看瞬间时启动弹出**通知权限请求**（红线：首存后才请求） |
| `step6-launch-stable.png` | Step 6 全部合入后启动稳定无崩 |

> **截图局限（重要）**：本机模拟器**没有 tap 自动化工具**（无 idb、simctl 无 tap 子命令）。所以只能截到"启动即可见"的主页；需要点击才能到达的页面——**回信段、明信片查看页、星空页、点亮日历、里程碑庆典、widget 桌面态**——无法headless 截图。这些都已构建通过 + 代码就绪，**需 Adam 在 Xcode/真机手动点开验收**（见待决清单）。

---

## 五、自行决策记录（睡前替 Adam 拍的板）

1. **不迁移 SwiftData store 到 App Group**。现状 ModelContainer 用默认位置；移库会换存储位置 = 模拟器历史测试数据消失（红线要保数据）。Widget 只读 app-group JSON 快照，不直连库 → 无需移库。
2. **一键存入走 app-group 写队列**（widget 写 `quickdeposit.queue.json` → App drain 建 Moment），而非"纯深链预填编辑器"。比深链更接近蔚来一键体验，且不碰 store。已端到端验证成功。
3. **回信/明信片用 Moment 加可选字段**（reply/repliedAt/postcardSeenAt）。SwiftData 加可选字段是兼容操作（Settings.swift 既有先例佐证），实测迁移不崩、历史数据不丢。
4. **星空 = 账户 Tab 入口卡**（不新增底 tab、不替代 AccountTabView）。点亮日历同样做成入口卡。最低打扰、可被发现。
5. **新文件落位避开 pbxproj 手术**：工程用 fileSystemSynchronizedGroups——app-only 新文件放进同步目录 `TimeBank/TimeBank/Retention/` 自动纳入 app target；共享契约扩展已双 target 的 `WidgetSnapshot.swift`；widget 渲染 + App Intent 写进已在 widget target 的 `TimeBankWidget.swift`。**全程零 pbxproj 改动**。
6. **Widget 配色**：widget target 不含 DesignTokens（且 DesignTokens 依赖 Dimension，整体引入会依赖爆炸）。→ 在 TimeBankWidget.swift 内建一份与 DesignTokens "magazineApartamento" 主题完全对齐的 `WT` token 表，消除散落硬编码。widget 进程读不到用户选的主题（UserDefaults 不共享），统一回落默认主题，视觉一致。
7. **状态机/轮换文案**先用了设计稿里的示例文案（观察式、无 guilt），最终需对照 `文案系统.md` 微调（见待决）。
8. **点亮日历的"点亮日"用 createdAt**（你来存入的那天）而非 happenedAt（瞬间发生的那天）——因为这是"陪伴/存入行为"的连续，不是"回忆日期"。
9. 验证用的测试数据：通过队列注入了 6 条瞬间（含一条 2025-06-11 的"一年前的今天"），方便截图与后续手验。**这些是真实落库的测试数据，Adam 若要干净环境可在模拟器删 App 重装。**

---

## 六、遗留问题 / 已知局限

1. **桌面 widget 真机轮换体验未截图验证**。逻辑（timeline 预排 48 entry + 状态机 + 一键存入按钮）已实现并构建通过，但模拟器加 widget 到桌面需手动操作，未截图。建议 Adam 真机/模拟器手动添加 medium + small + 锁屏三个 widget 观察。
2. **明信片 widget 面不含图片**。widget 进程读不到 App 沙盒里的媒体文件（不在 app-group），所以 widget 的"记忆/明信片"面只显示标题+天数，不显示缩略图。要让 widget 显示图，需把缩略图拷进 app-group——列为 P1（设计文档已注明）。
3. **通知深链的前台呈现**：当前 willPresent 返回 banner+sound；点开 banner 路由到明信片 sheet 的链路已写但未真机验证（需要等通知真的在 2-4 天后触发，或手动用 `simctl push` 模拟）。
4. **里程碑庆典未触发**：当前测试数据 6 条 < 10，未到第一个里程碑。逻辑 + UserDefaults 去重已就绪，需凑满 10 条触发验证。
5. **onboarding 从相册预填 2-3 格（禀赋进度）未做**：本期只做了"运行时冷启动兜底"（0/少记忆时 widget/卡片不空、不尴尬）。onboarding 预填改动面更大，设计文档已列为 P1。
6. **连续奖励的星空特效**目前是"近 7 天≥3 笔触发流星"的轻量版；设计表里的"连续 3/7/21 次解锁特制明信片/册页"尚未全部接线（明信片皮肤池、册页解锁未做），属 P1 细化。
7. **点亮日历只渲染当月**（+最近八周条）。跨月翻看未做，P1。
8. UI 测试 runner 启动失败告警（疑似被通知权限弹窗挡住），单元测试通过；建议授予/重置通知权限后重跑。

---

## 七、给 Adam 的待决清单

**A. 需要你手动点开验收的页面**（模拟器无 tap 工具，我截不到）：
1. 在 Xcode 跑起来 → 点主页「今日回忆卡」→ 看**瞬间详情页底部「现在的我想说」回信**交互（邀请→编辑→存下→显示日期）。
2. 账户 Tab → 点「**星空账本**」入口卡 → 看 Canvas 星空（每账户一星座、流星）。
3. 账户 Tab → 点「**点亮日历**」入口卡 → 看月历点亮日 + 最近八周留白条 + 文案「空着的那周我们替你留着」。
4. 桌面/锁屏**添加三个 widget**（systemMedium / systemSmall / 锁屏矩形），观察状态机随时间变化 + 点 medium 上的「存入此刻」按钮（应即时变 ✦，回 App 后落库）。
5. 明信片查看页：当今日回忆卡命中"刚冲洗好（2-4 天前未看）"的瞬间时，点开应进**明信片样式页**而非普通详情。

**B. 需要你定夺的产品/文案：**
1. **文案终审**：widget 状态机 4 句、明信片通知句、回信邀请句、点亮日历文案、里程碑庆典句——我用的是设计稿示例，请对照 `文案系统.md` tone 拍最终版。
2. **明信片 widget 要不要显示缩略图**？（要的话需把缩略图拷进 app-group，P1 工作量）。
3. **连续奖励要做到多细**？现在是"流星轻量版"；要不要接"连续 7 次解锁特制明信片 / 21 次特效 / 第 100 张册页"全套（P1）。
4. **onboarding 相册预填 2-3 格**（禀赋进度，34% vs 19% 那个）要不要做进 V1（P1，改 onboarding）。
5. **一键存入落库账户**：现在落到"首个可见可写账户"（你的环境是陪伴侣）。要不要让用户在 widget 配置里选默认账户？（IntentConfiguration，P1）

**C. 合并建议**：分支 `claude/retention-p0` 已自包含、未 push。建议你先在模拟器手验 A 清单，确认体验 OK 后再决定是否 push / 合 main。我没有自动 push（按红线"全程不 push、不动 main"）。

---

## 八、关键文件清单（本次改/增）

**改：**
- `TimeBank/Shared/WidgetSnapshot.swift`（快照加 todayDeposited/memories + 一键存入队列类型）
- `TimeBank/Shared/WidgetSnapshotWriter.swift`（填充今日已存入 + 记忆池）
- `TimeBank/Shared/MomentStore.swift`（drainQuickDepositQueue）
- `TimeBank/Models/Moment.swift`（reply/repliedAt/postcardSeenAt）
- `TimeBank/App/RootView.swift`（注册通知 delegate + 前台 drain + 排明信片通知）
- `TimeBank/Features/Home/HomeView.swift`（今日回忆卡 + 明信片/里程碑 sheet）
- `TimeBank/Features/Moment/MomentDetail/MomentDetailView.swift`（回信段）
- `TimeBank/Features/Account/AccountTabView.swift`（星空 + 点亮日历入口卡）
- `TimeBankWidget/TimeBankWidget.swift`（状态机 + 轮换 + 三尺寸 + token + App Intent）

**增（`TimeBank/TimeBank/Retention/`，同步目录自动纳入 app target）：**
- `TodayMemory.swift` / `TodayMemoryCardView.swift`（盲盒选片 + 卡片）
- `PostcardNotifications.swift` / `PostcardView.swift`（明信片通知 + 查看页）
- `StarFieldView.swift`（星空）
- `StreakModel.swift` / `LitCalendarView.swift` / `MilestoneCelebrationView.swift`（连续奖励）

---

## 九、第二轮（06-11 上午 · Adam 拍板后）

Adam 对待决清单的决定：①文案终审要做 ②widget 缩略图要 ③连续奖励接全套 ④onboarding 预填要 ⑤一键存入不做可配置。四项已全部实现（4 个 commit，每步构建通过，单元测试 TEST SUCCEEDED）：

| commit | 内容 |
|--------|------|
| `933dfd9` | **文案终审**：对齐文案系统 tone——去掉品牌自称（"我们"→"这里"、"时间银行"→"过去的你"）、widget 文案池替换为今日觉知池 canon 句、通知去冗余标题 |
| (C2) | **widget 记忆面缩略图**：Writer 把入选 6 条记忆的缩略图降采样（≤360px JPEG）导出到 App Group `widget-thumbs/`，widget 轮换面渲染小图；全链路静默降级、自动清理旧图 |
| `e45f438` | **连续奖励全套**（设计 §4.2）：≥3 次星空呼吸增强 / ≥7 次解锁「七次之后」金色纪念明信片皮肤（按瞬间 id 稳定命中约 1/3）/ ≥21 次流星 + 星座连线辉光。累计宽松计，只奖不罚 |
| (C4) | **onboarding 开户首存**：Step4 加「如果你愿意，先存一笔」卡，相册选 1-3 张旧照（EXIF 拍摄日期，读不到用当天），finalize 后自动存为第一批瞬间（禀赋进度 34% vs 19%） |

**注意**：当前模拟器 7 条测试数据都是纯文字（无照片），所以 widget 缩略图和明信片照片面在真机/带图数据下才能看到效果——存一条带照片的瞬间即可验证。
