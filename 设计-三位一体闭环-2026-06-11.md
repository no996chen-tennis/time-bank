# 三位一体留存闭环 · 统一设计

> 2026-06-11 · Claude 自主任务产出（Adam 睡眠时）。本文是**动代码前的总纲**，把 Adam 拍板保留的三套（叁 记忆盲盒/明信片 · 肆 星空账本 · 伍 签到台）串成**一个闭环、不是三个割裂功能**。下游所有实现以本文为 spec。
>
> 输入：`方案-留存五方案-2026-06-10.md`、`调研-留存机制-2026-06-10/` 四份、`designs/2026-06-10-五方案/index.html`、`文案系统.md`、现有代码（SwiftData + WidgetKit）。
>
> 红线（全程遵守）：只奖不罚 · 中断不清零不显示断签 · 自动补灯 · 无数字角标/无恐吓倒计时/无限时窗口 · 消耗层不做最后一屏 · 颜色字号全走 DesignTokens · 不联网不引第三方 · 文案对照 `文案系统.md` 的"温柔·成熟·不煽情·观察式"。

---

## 0. 一句话闭环

**签到台（入口）让你几乎零成本地存入 → 每一笔存入当场点亮一颗星（看得见的资产）→ 几天后这笔记忆冲洗成明信片、并在某个清晨从盲盒里回到你面前 → 你对当时的自己回一句话 → 这句回信本身又是一次新的存入。** Widget 是贯穿始终、永远在线的中枢，把上面每一站的状态轮流端到你眼前。

三套不是三个 tab，而是同一份 `Moment` 数据在**五个工位**上的不同呈现。没有任何一套能单独成立：星空没有存入就没有星；盲盒没有积累就没有可回流的记忆；签到台没有回报就只是个打卡器。

---

## 1. 闭环图（数据怎么流）

```
                         ┌───────────────────────────────────────────────┐
                         │                                               │
                         ▼                                               │
   ① 签到台 / Widget ───────────────► ② 存入 Deposit ───────────────► ③ 星空 Star
   （触发 / 入口）                       （投入 / Action）                 （资产 / 看得见的积累）
   桌面 medium+small                    · 一键"此刻"(widget 直存)           每笔 Moment = 一颗星
   锁屏 rectangular                     · 完整编辑器(图文)                  存入当场"点亮"动画 = 即时回报
   · 今日存入状态机                      └─ 写 Moment → SwiftData            连续存入触发星空特效
   · 余额一句话                             → WidgetSnapshotWriter            （只奖不罚）
   · 轮换：一颗星的回忆                       → app-group JSON 快照
   · 轮换：冲洗好的明信片                      → WidgetCenter.reload          │
   · 一键存入按钮(App Intent)                                               │
        ▲                                                                  ▼
        │                                                       （数日沉淀 · 时间复利）
        │                                                                  │
        │                                                                  ▼
   ⑤ 回信 Reply ◄──────────── ④ 盲盒回看 + 明信片冲洗 ◄──────────────────────┘
   （再投入 = 新的存入）          （不可预测的回流 / 多变奖励 + 延迟礼物）
   瞬间详情页                     · 今日回忆卡（日期种子稳定随机，周年/同月日优先）
   「现在的我想说」                · 明信片：存入后 2-4 天本地通知"冲洗好了"+ 明信片查看页
   写下 → 追加为                   · widget 轮换面之一就是"一颗星的回忆 / 冲洗好的明信片"
   该 Moment 的回信               └─ 点开回看 → 勾起"现在想说什么" → ⑤
        │
        └──────────────────────────►（回信落库 = 又一次投入，回到 ②，飞轮再转一圈）
```

### 每个工位的角色 + 数据契约

| 工位 | 闭环角色（Hooked 模型） | 读什么 | 写什么 |
|------|------------------------|--------|--------|
| ① 签到台 Widget | **外部触发**（把"打开"变成被情境唤起） | app-group `snapshot.widget.json`（余额/今日已存入/轮换内容池） | 一键存入 → app-group `quickdeposit.queue.json` |
| ② 存入 Deposit | **行动 + 投入** | Dimension 列表 | `Moment`（SwiftData）→ 触发快照重写 + widget reload |
| ③ 星空 Star | **即时回报**（峰终封存仪式 / Fog of War） | 按 dimension 分组的 `Moment`（只渲染已点亮） | 无（纯读，连续奖励状态由 Moment 派生） |
| ④ 盲盒 + 明信片 | **多变奖励 + 延迟礼物**（Appointment + 怀旧） | `Moment`（日期种子选一条；createdAt 2-4 天前且未冲洗的选明信片） | `postcardSeenAt`（标记已冲洗查看） |
| ⑤ 回信 Reply | **再投入**（老内容产生新内容 = 下一次触发） | 单条 `Moment` | `Moment.reply` / `repliedAt`（新增可选字段） |

**数据主干：一切都是 `Moment`。** `WidgetSnapshotWriter` 把 Moment 投影成 `TimeBankWidgetSnapshot`（app-group JSON）→ Widget 渲染。这套契约已存在，本次只做**加法**（见 §7）。

---

## 2. 盲盒 / 明信片 嵌入位置

### 2.1 今日回忆卡（盲盒）——主页 hero 下第一张卡
- **位置**：`HomeView.homeContent` 里，`DoubleLayerAccountCardView`（存折 hero）下方、`dimensionSectionHeader("时间账户 · N 个")` 上方。它是"打开 App 第一眼就有东西等我"（Appointment Mechanics，本调研最重要单点）。
- **选片算法**：以**当天日期为种子**（`Calendar` 的 year-month-day 拼成 Int seed）做稳定随机——同一天多次打开是同一条，次日才换。优先级：① 去年/历年**同月同日**的瞬间（"那年今日"）② **同月日**近似 ③ 纯随机。用种子保证稳定，避免"刷新式"消耗。
- **卡片内容**：缩略图（若有 media）+ 标题 + "N 天前 / 一年前的今天" + 账户色书脊。点击进 `MomentDetailView`。
- **冷启动态**：见 §6。

### 2.2 明信片冲洗——延迟礼物
- **触达**：存入后 **2-4 天**（随机落点，制造不可预测）排一条本地通知"你存的那个瞬间，冲洗好了"。通知点开深链到明信片查看页。通知遵守红线：每周总量≤4、今日已存入则当晚不推、首次存入完成后才请求通知权限。
- **查看页**：`PostcardView`——明信片视觉（参考 demo：邮票、邮戳、节气/日期脚注、手写体标题）。正面=照片，翻面/下方=note + 日期戳。看完标记 `postcardSeenAt`，不再重复推。
- **与盲盒的关系**：明信片是"刚冲好的盲盒"。今日回忆卡若命中一条"刚冲洗好（2-4 天且未看）"的瞬间，就以**明信片皮肤**呈现；否则以普通回忆皮肤呈现。一套数据，两种成色。

### 2.3 回信入口——瞬间详情页
- **位置**：`MomentDetailView` 底部（media + note 之后），加一段「**现在的我想说**」。
  - 无回信：一行温柔邀请 + 输入入口（"想对当时的自己说点什么吗？"）。
  - 有回信：展示回信文本 + 回信日期（"2026-06-11 的你写道：…"）。可再编辑。
- **落库**：新增 `Moment.reply: String?` + `repliedAt: Date?`（SwiftData 加可选字段是兼容操作，见 Settings.swift 既有注释佐证；不迁移 store、不丢测试数据）。回信写入即"再投入"，可触发一次轻封存动画，并刷新快照（让 widget 的"那条记忆"带上"你回过信"的微标）。

---

## 3. 星空 嵌入位置

**结论：账户 Tab 顶部"星空"入口卡 → push 全屏 `StarFieldView`。不新增底 tab、不替代 AccountTabView。**

- **理由**：底 tab 只有 home/account/me 三个，加第四个会稀释；AccountTabView 已经按 dimension 聚合 Moment，星空就是同一份数据的"诗意可视化"，做成入口卡最低打扰、且可被发现。
- **入口卡**：AccountTabView 顶部一张"星空"卡，副文案随累计变化（"你已点亮 27 颗星"）。
- **`StarFieldView`**：每个账户一个**星座**，每条 Moment 一颗**星**（SwiftUI `Canvas` 即可）。**只渲染已点亮的星**（绝不显示总数/剩余/未解锁灰格——红线）。星的位置用 Moment.id 做稳定散布，亮度/大小可按 media 数或 duration 微调。
- **即时回报复用**：存入成功后，在主页/详情页播一个"光点落入账户"的轻动画（峰终定律封存仪式），不必每次进星空页——星空页是"资产全貌"，封存动画是"刚点亮的那一颗"。
- **连续奖励特效**：见 §4，特效在星空页呈现（流星/星座整体辉光）。

---

## 4. 连续存入奖励（Adam 点名）——只奖不罚

**单位 = 周，不是天。** 依据：Lyubomirsky (2005) 感恩练习每周一次提升幸福感、每天三次反而无效（感恩疲劳）；本产品"每周存几个就很好"。所以**连续的最小颗粒是"被点亮的周"**，不是"连续天数"。

### 4.1 点亮日历 + 自动补灯
- **点亮日历**：日历视图，有存入的"日"点亮，有存入的"周"标为"点亮周"。文案只用累计正向框架："六月你点亮了 14 个晚上""陪伴你的第 47 周 · 共 132 个瞬间"。
- **永不显示**：连续 N 天、断签天数、灰色未解锁。
- **自动补灯（Streak Freeze 温柔版）**：某周 0 存入，系统**自动垫一格"留白周"**——免费、静默、零操作、永不清零。中断在视觉上不存在（链条始终连续），只是那一格是"留白"皮肤而非"点亮"皮肤。Duolingo Streak Freeze 实证濒临断签流失 -21%；我们做成全自动、不消耗、不提醒的版本。

### 4.2 奖励表（只增不减；"连续 N 次"按**存入次数**宽松计，断了不归零、由补灯续上）

| 触发条件 | 奖励 | 呈现位置 | 红线校验 |
|---------|------|---------|---------|
| 连续 **3** 次存入 | 星空轻微 shimmer（星点呼吸增强一档） | 星空页 + 存入封存动画 | 不弹窗、不打断 |
| 连续 **7** 次存入 | 解锁一张**特制明信片/册页**（"七次"纪念皮肤） | 进盲盒/明信片池 | 是给予不是任务 |
| 连续 **21** 次存入 | 较强星空特效（一颗**流星**划过 / 当前账户星座整体辉光） | 星空页 | 一次性庆祝，可重看 |
| 第 **10 / 50 / 100** 个瞬间（**累计**，永不归零） | **里程碑庆典页**：你自己照片的蒙太奇 + 一句回顾性措辞（"第 100 个被你认真留下的瞬间"） | 全屏庆典（存入后触发一次） | 用回忆性措辞，绝不打分/排行 |

- **"连续"的宽松定义**：以"被点亮的周"为链，自动补灯让链永不断；里程碑只看**累计瞬间数**（断签完全不影响）。所谓"连续 3/7/21 次"是**近期存入的滚动计数**带来的额外彩蛋，断了不惩罚、不显示、下次存入继续累加，由补灯在视觉上保持连续。
- **fresh start**：若长期未存，只在周一/月初/生日**温柔重启**（"之前的 132 个瞬间都还在"），绝不在中途敲打。

---

## 5. Widget 矩阵与内容轮换

### 5.1 三尺寸各显示什么

| 尺寸 | 主体 | 轮换面 | 交互 |
|------|------|--------|------|
| **桌面 systemMedium** | 今年余额 N 周 + 3 个账户 mini 卡 + 已存入 N 个瞬间 | 顶部一句话在内容池轮换 | iOS17 一键存入按钮（App Intent） |
| **桌面 systemSmall** | 今年余额 N 周（大数字）+ 今日状态机一行 | 整个下半区在内容池轮换 | 整块点击进 App（small 不放按钮，避免误触） |
| **锁屏 accessoryRectangular** | 第一行=今日状态机 / 第二行=余额或一条记忆 | 第二行轮换 | 点击进 App |

> （accessoryCircular"今日已存入圆环"列为 P1，本期先交付 rectangular，符合任务清单。）

### 5.2 内容池（轮换面 face pool）
四类面，轮流出现：
1. **今日状态**（状态机，见 5.3）
2. **一颗星的回忆**（从已存入里按日期种子选一条："一年前的今天 · 晚饭后的散步"）
3. **余额一句话**（`文案系统.md` §三 轮换池，观察式）
4. **冲洗好的明信片**（若存在 2-4 天前、未看的瞬间，端出"你存的那个瞬间冲洗好了"）

### 5.3 今日存入状态机（timeline 预排，零刷新成本）
确定性时间函数，全部预排进 timeline，不消耗刷新预算：

| 时段 | 状态文案（示例，最终对照文案系统调） |
|------|--------------------------------------|
| 00:00 重置 | "新的一天。" |
| 白天（平静） | "今天也可以存下一段。" |
| 18:00 后（温柔） | "天黑了，今天有什么想留下的？" |
| 21:30 后（睡前邀请） | "睡前，留一句给今天？" |
| **存入后（任何时刻）** | "今天已存入 ✦"（当晚不再催） |

### 5.4 轮换机制 + iOS 物理极限（重点回答）
- **机制**：`getTimeline` 一次性**预排约 48 条 entry**（每 30 分钟一条，覆盖未来 ~24h）。每条 entry 的"轮换面"= 按 entryIndex 在内容池里推进（确定性，带权重：状态机面在傍晚/睡前加权）。**系统按预排时间自动切换 entry，零刷新预算成本**——因为时间流逝是确定的，`Text(timerInterval:)` 同理。
- **主动 reload**：用户打开 App（或一键存入）时调 `WidgetCenter.reloadAllTimelines()` → 重新随机化内容池 + 翻转"今日已存入"。这类 reload 由**用户行为驱动，不算进系统后台预算**。
- **每日策略刷新**：timeline policy `.after(次日 04:00)`，凌晨重排（重置今日状态 + 换一批记忆）。
- **iOS 刷新预算（~40-70 次/天）下，"每次看都不同"能逼近到什么程度**：
  - **能做到**：一天之内**免费切换 ~48 次面**（预排 entry，不花预算）——即你在不同半小时窗口瞥一眼，看到的面不同。
  - **做不到**：真正"每一次抬腕都重新随机"——那需要每次 glance 都 reload（=花预算），40-70 次/天的预算撑不住"每秒/每分钟变"。
  - **结论**：靠"预排 48 面 + 每次打开 App 时 reload 重随机"两条腿，实际体验非常接近"每次看都不同"：半小时粒度自动轮换 + 每次用完 App 回到桌面又是新的一批。这是 iOS 上的物理最优解，且**零额外耗电、零联网**。

---

## 6. 冷启动（记忆很少时 widget / 盲盒显示什么）

| 阶段 | 判定 | Widget | 今日回忆卡（主页） |
|------|------|--------|-------------------|
| **0 条** | 无任何 Moment | 余额 + **邀请文案池**（今日觉知池，如"过去 24 小时有什么值得存下来的？"）+ 一键存入更突出 | 邀请态："存入第一个，这里会长出回忆。"+ CTA `存入第一个` |
| **1-5 条** | 有 Moment 但无 >7 天的旧记忆 | 余额 + **短周期回忆**（"X 天前的今天"/最近存的一条，没有"一年前"就用最近）+ 状态机 | "X 天前的今天"短周期回忆；引导补充（可选：onboarding 从相册预填 2-3 格做禀赋进度，见下） |
| **正常** | 记忆 ≥ 6 条 **且** 存在 >7 天的旧记忆 | 完整轮换池（那年今日/明信片/余额/状态机） | 日期种子选片，周年优先 |

- **onboarding 禀赋进度（推荐 / P1）**：onboarding 末尾从相册预填 2-3 格图鉴/星空（Nunes & Drèze 洗车卡：预盖 2 格完成率 34% vs 19%）。本期 P0 先保证**运行时冷启动兜底**（widget/卡片在 0/少记忆时不空、不尴尬）；onboarding 预填作为 P1（改动面更大，单独切）。
- **"那年今日"第一年没有去年数据**：用"上周的今天 / X 天前的今天"兑现（调研 §五落地点 1）。

---

## 7. 实现切片与优先级（对应第二阶段 Step 0-6）

### 关键技术决策（已自主拍板，写进进度报告）
1. **不迁移 SwiftData store 到 App Group**。现状 `ModelContainer` 用默认位置（非 group），widget 只读 app-group 的 JSON 快照、不直连 store。移 store 会换库位置 = 模拟器历史测试数据"消失"（红线：要保历史数据）。→ **保持现状**。
2. **一键存入用 app-group 写队列，不共享 store**：widget 的 App Intent 把一条 `QuickDepositRequest` 写进 app-group `quickdeposit.queue.json` + 翻转快照里"今日已存入" + reload；App 启动/前台时 **drain 队列** 建真 `Moment`（首个核心账户、标题"此刻"、时长 0）。这样：widget 即时反馈 + 真·不开 App 存入 + 数据最终落库且不丢（队列本身就在 app-group 持久化）。比"纯深链预填编辑器"更接近蔚来一键体验；若实现中 SwiftData 侧 drain 成本过高，再降级为深链并在报告说明。
3. **回信用 `Moment` 加可选字段** `reply/repliedAt`（加可选字段是 SwiftData 兼容操作，不丢数据）。明信片用 `postcardSeenAt: Date?` 标记。
4. **新文件落位**（避免 pbxproj 手术，已验证工程结构）：
   - App-only 新文件 → 放进**同步目录** `TimeBank/TimeBank/Retention/`（app target 用 fileSystemSynchronizedGroups，自动纳入，零 pbxproj 改动）。
   - App↔Widget **共享契约** → 扩展已双 target 的 `TimeBank/Shared/WidgetSnapshot.swift`（加快照新字段 + 队列类型）。
   - Widget 渲染 + App Intent → 写进已在 widget target 的 `TimeBankWidget/TimeBankWidget.swift`。
5. **Widget 配色**：widget target 不含 DesignTokens（且 DesignTokens 依赖 `Dimension`，整体引入会依赖爆炸）。→ 在 TimeBankWidget.swift 内建一份**与 DesignTokens magazine 主题完全对齐的 token 表**（单一来源，消除散落的 `Color(red:…)` 硬编码），并注释指向 DesignTokens 为真源。widget 进程读不到用户选的主题（UserDefaults 不共享），统一回落到 app 默认主题 magazineApartamento，视觉一致。

### Step 列表（每步：构建通过 + 截图 + commit）

| Step | 内容 | 落点 | 优先级 |
|------|------|------|--------|
| 0 | 切 `claude/retention-p0`；commit 工作树方案壹存折改动 | git | 必做先做 |
| 1 | **Widget 全家桶**：medium+small+锁屏 rect；状态机 timeline 预排；内容轮换；色走 token 表；snapshot 补"今日已存入"+轮换池 | TimeBankWidget.swift + WidgetSnapshot.swift + WidgetSnapshotWriter.swift | P0 最高（根治忘记打开） |
| 2 | **一键存入** App Intent（app-group 队列 + App 端 drain） | TimeBankWidget.swift + WidgetSnapshot.swift + MomentStore.swift + RootView.swift | P0 |
| 3 | **今日回忆卡（盲盒）+ 回信**：主页卡 + 详情页回信 + 冷启动兜底 | Retention/ 新文件 + HomeView.swift + MomentDetailView.swift + Moment.swift | P0 |
| 4 | **明信片冲洗**：本地通知（2-4 天）+ 明信片查看页 | Retention/ 新文件 + Moment.swift（postcardSeenAt） | P1 |
| 5 | **星空页**：账户 Tab 入口卡 + StarFieldView（Canvas） | Retention/ 新文件 + AccountTabView.swift | P1 |
| 6 | **连续奖励**：点亮日历 + 自动补灯 + 里程碑庆典 | Retention/ 新文件 | P1 |

> 配额可能中途耗尽：**做完一步算一步**，每步独立 commit，保证已完成步骤可用。Step 1/2（widget + 一键存入）是闭环的"入口 + 行动"，价值最高，优先保证。

---

## 8. 验收对照红线（自检表）

- [ ] 无任何"连续 N 天"/断签天数/灰色未解锁/数字角标/恐吓倒计时（"又少了 X 小时"）
- [ ] 中断不清零、自动补灯
- [ ] 通知：每周≤4、今日已存入当晚不推、首次存入后才请求权限
- [ ] 消耗层信息不做任何流程最后一屏
- [ ] 颜色/字号走 token；不联网、不引第三方
- [ ] 新文案对照 `文案系统.md`（观察式、不评判、不煽情）
