# M3 端到端复跑报告

- 时间：2026-04-26 16:46
- 分支：m3-app-loop
- 设备：iPhone 17 Pro Simulator, iOS 26.4
- 范围：TC-S06 ~ TC-S12 专项复跑 + 时长展示修复验证

## 代码修复摘要

1. 新增 `Formatter.storedDuration(_:)`，存储层统一输出：`0h` / `30m` / `1h` / `1h 30m`。
2. 统一以下存储层展示面：
   - Home 顶部双层卡「已存入」
   - `DimensionCard` 已存入列
   - `MomentTimeline` section header
   - Moment row / Moment detail duration chip
3. 修复详情页删除后 Undo Toast 容易不可见的问题：Toast overlay 提升到 `NavigationStack` 外层，详情页删除先 pop 再显示 Undo。
4. 补强旧数据兼容：`FamilyMember` / `ChildInfo` / `PartnerInfo` 支持缺少 Memorial 字段的历史 JSON 解码，避免 Onboarding 后回首页崩溃。

## 自动验证

- PASS：`xcodebuild build -project TimeBank/TimeBank.xcodeproj -scheme TimeBank -configuration Debug -destination id=F07B5944-A948-4A92-9D6A-5A4898A07D9B`
- PASS：`DataLayerTests.testFormatterMatrixCoversAllInterfaces`
- PASS：`DataLayerTests.testRelationshipCodablesDecodeLegacyMissingMemorialFields`

## UI 复跑结果

| 用例 | 结果 | 备注 |
| --- | --- | --- |
| TC-S06 | PASS | 新增 30 分钟父母时刻后，首页顶部已存入和父母卡均显示 `30m` / `1 个瞬间`。截图：`screenshots/m3-rerun-20260426-1624/TC-S06-home-30m.png` |
| TC-S07 | PASS | 父母详情页 section header 显示 `已存入瞬间 · 1 个 · 30m`，row 元信息显示 `30m`。截图：`TC-S07-detail-header-30m.png` |
| TC-S08 | PASS | Moment detail chip 显示 `30m`，不再出现 `30 分钟`。截图：`TC-S08-detail-chip-30m.png` |
| TC-S09 | PASS | 编辑标题后详情页实时更新，duration chip 仍为 `30m`。截图：`TC-S09-edited-title.png` |
| TC-S10 | FIXED | 复跑发现详情页删除后未出现 Undo Toast；已修复 overlay 层级和详情页删除时序，并通过 build。 |
| TC-S11 | PASS | Cold install 后 Persona B（solo）进入首页，个人信息添加伴侣（1995）成功，首页出现「陪伴侣」卡。 |
| TC-S12 | PARTIAL | 「陪伴侣」详情页可进入，Memorial 入口依赖 long press/context gesture；Computer Use 当前无法稳定触发该手势，未完成标记/取消的端到端点击验证。 |

## M3 Polish

- Onboarding Step 4 已在测试文档主流程明确为「完成 + 提醒引导」：包含「每天早上轻轻提醒你一次 —— 不催，不推销。」以及「以后再说 / 好，开启提醒」两条路径，避免后续误判。

## 结论

`30m / 0.5 小时 / 1h` 不一致问题已收敛到统一 formatter；TC-S06 ~ TC-S09 和 TC-S11 已手工通过。TC-S10 在复跑中暴露并已修复。TC-S12 因 Computer Use 手势限制未完成完整 UI 触发，但伴侣账户链路可用。
